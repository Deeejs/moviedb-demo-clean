# Monorepo Configuration
# Enable workspace hoisting for shared packages
hoist-pattern[]=*

# Auto-install peer dependencies
auto-install-peers=true

# Use exact versions for consistency across environments
save-exact=true