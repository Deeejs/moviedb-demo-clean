meta {
  name: Delete Actor
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/actors/{{actorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiToken}}
}

docs {
  Delete an actor (requires authentication)
}